# Contributing

Suggest one focused, maintained project at a time. Include its license, a
copy-paste install command, what artifact it produces, and whether it uploads
workspace data. Run `npm run validate` and `npm test` before opening a pull request.
